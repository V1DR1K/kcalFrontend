import React, { useCallback, useEffect, useState } from "react";
import { Header, Panel } from "../../components/Layout";
import { Icon } from "../../components/Icon";
import "../../styles/05-scanner.css";
import "../../styles/06-history.css";
import { CatalogStatus } from "../catalog/CatalogComponents";
import { formatNumber, readableDate } from "../../utils/format";
import { HistoryDayPreview, HistoryExportDialog } from "./dialogs/HistoryDialogs";

export function History({ api }) {
  const [data, setData] = useState(null); const [loading, setLoading] = useState(true); const [error, setError] = useState(""); const [selectedDay, setSelectedDay] = useState(null); const [exportOpen, setExportOpen] = useState(false); const [exporting, setExporting] = useState(false); const [monthOffset, setMonthOffset] = useState(0);
  const currentDate = new Date(); const viewDate = new Date(currentDate.getFullYear(), currentDate.getMonth() + monthOffset, 1);
  const load = useCallback(() => { setLoading(true); setError(""); api.runAction({ title: "Cargando historial", description: "Estamos preparando tu calendario..." }, () => api.request(`/api/nutrition/history?year=${viewDate.getFullYear()}&month=${viewDate.getMonth() + 1}`)).then(setData).catch(() => setError("No pudimos cargar tu historial.")).finally(() => setLoading(false)); }, [api, viewDate.getFullYear(), viewDate.getMonth()]);
  useEffect(load, [load]);
  if (loading) return <section className="page"><Header title="Historial" /></section>;
  if (error) return <section className="page"><Header title="Historial" /><CatalogStatus error>{error}<button className="secondary" onClick={load}>Reintentar</button></CatalogStatus></section>;
  const rawMonthLabel = new Intl.DateTimeFormat("es-AR", { month: "long", year: "numeric" }).format(viewDate); const monthLabel = rawMonthLabel.charAt(0).toUpperCase() + rawMonthLabel.slice(1); const leadingDays = (new Date(viewDate.getFullYear(), viewDate.getMonth(), 1).getDay() + 6) % 7;
  return <section className="page"><Header title="Historial" /><div className="grid two history-summary"><Panel title="Promedio del mes"><p className="big">{formatNumber(data?.averageCalories)} kcal</p></Panel><Panel title="Días con objetivo cumplido"><p className="big">{data?.completedGoalDays || 0} días</p></Panel></div><div className="history-calendar-toolbar"><button className="primary calendar-export" type="button" onClick={() => setExportOpen(true)}><Icon name="download" />Exportar a Excel</button></div><div className="history-calendar-shell"><div className="history-calendar-main"><div className="calendar-heading"><button className="secondary calendar-nav" onClick={() => setMonthOffset((offset) => offset - 1)}><Icon name="chevron_left" />Anterior</button><div><h2>{monthLabel}</h2><span>Tu constancia, día por día</span></div><button className="secondary calendar-nav" onClick={() => setMonthOffset((offset) => offset + 1)} disabled={monthOffset >= 0}><Icon name="chevron_right" />Siguiente</button></div><div className="calendar-weekdays" aria-hidden="true">{["L", "M", "X", "J", "V", "S", "D"].map((day) => <span key={day}>{day}</span>)}</div><div className="calendar-grid">{Array.from({ length: leadingDays }, (_, index) => <span className="calendar-spacer" key={`spacer-${index}`} />)}{(data?.days || []).map((day) => <button type="button" key={day.date} className={day.goalReached ? "done" : ""} style={{ "--plan-color": planColor(day.planId || day.planName) }} title={`Ver detalle del ${readableDate(day.date)}`} aria-label={`${readableDate(day.date)}, ${day.goalReached ? "objetivo cumplido" : "día registrado"}. Ver detalle`} onClick={() => setSelectedDay(day)}><b>{new Date(`${day.date}T00:00:00`).getDate()}</b><small>{day.planName}</small>{day.goalReached && <Icon name="check_circle" />}</button>)}</div><div className="plan-legend">{[...new Map((data?.days || []).map((day) => [day.planId || day.planName, day])).values()].map((day) => <span key={day.planId || day.planName}><i style={{ background: planColor(day.planId || day.planName) }} />{day.planName}</span>)}</div></div><aside className="history-calendar-aside"><Icon name="calendar_month" /><h3>Un día a la vez</h3><p>Elegí cualquier fecha para revisar tus comidas, macros e hidratación con el contexto completo.</p><div><span><i className="history-status-dot complete" />Objetivo cumplido</span><span><i className="history-status-dot recorded" />Día registrado</span></div></aside></div>{selectedDay && <HistoryDayPreview api={api} day={selectedDay} onClose={() => setSelectedDay(null)} />}{exportOpen && <HistoryExportDialog api={api} monthDate={viewDate} exporting={exporting} setExporting={setExporting} onClose={() => setExportOpen(false)} />}</section>;
}

function planColor(value) { const palette = ["#4edea3", "#89ceff", "#ffd166", "#c7a6ff", "#ff8fa3"]; const hash = String(value || "plan").split("").reduce((sum, char) => sum + char.charCodeAt(0), 0); return palette[hash % palette.length]; }
